# Oracle DB - local
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@192.168.0.237:1522/XEPDB1}
spring.datasource.username=${DB_USERNAME:hr24}
spring.datasource.password=${DB_PASSWORD:hr24}

# Redis - local
spring.data.redis.host=${REDIS_HOST:192.168.0.237}
spring.data.redis.port=${REDIS_PORT:6379}

# Google SMTP Mail - local
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# \uBA54\uC77C \uBC1C\uC1A1 \uAE30\uB2A5 \uC0AC\uC6A9 \uC5EC\uBD80 - local
app.mail.enabled=${MAIL_ENABLED:false}
app.mail.from=${MAIL_FROM:no-reply@24hr.local}

# JWT - local
jwt.secret=${JWT_SECRET:hr24-jwt-secret-key-hr24-jwt-secret-key-hr24}
jwt.access-token-expiration=${JWT_ACCESS_TOKEN_EXPIRATION:900000}
jwt.refresh-token-expiration=${JWT_REFRESH_TOKEN_EXPIRATION:604800000}

# \uC554\uD638\uD654 \uAC1C\uBC1C\uC6A9 \uD0A4
hr24.crypto.secret-key=${HR24_CRYPTO_SECRET_KEY:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=}

# JPA - local
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:none}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Spring Doc - local
springdoc.api-docs.enabled=${SPRINGDOC_ENABLED:true}

# File Upload - local
file.upload-path=${FILE_UPLOAD_PATH:C:/hr24/uploads}

# Multipart - local
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:100MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:500MB}

# \uACF5\uACF5\uB370\uC774\uD130\uD3EC\uD138 \uACF5\uD734\uC77C API - local
holiday.api.key=${HOLIDAY_API_KEY:}

# CORS / WebSocket - local
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Refresh Token Cookie - local
app.cookie.secure=${COOKIE_SECURE:false}
app.cookie.same-site=${COOKIE_SAME_SITE:Lax}